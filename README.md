# VehShop — Интернет-магазин транспорта

[**Открыть демо на Vercel**](https://veh-shop.vercel.app/)

## Функциональность

### Каталог и Товары

- **Просмотр списка**: Витрина товаров с изображениями и ценами.
- **Детальная карточка**: Полная информация о товаре, галерея изображений с выбором превью, технические характеристики.
- **Отзывы**: Блок отзывов покупателей (подгружаются динамически).

### 🛒 Корзина

- **Управление**: Добавление товаров, изменение количества, удаление позиций.
- **Персистентность**: Состояние корзины сохраняется в `localStorage`, поэтому данные не теряются при перезагрузке страницы.
- **Расчеты**: Автоматический подсчет общей суммы и количества товаров.

### 👤 Личный кабинет

- **Профиль**: Отображение данных пользователя (аватар, имя, контакты).
- **Адрес и Оплата**: Визуализация адреса доставки и привязанных методов оплаты (карты, криптокошельки).
- **Настройки**: Возможность смены темы оформления.

### 🎨 UI/UX

- **Темы**: Светлая и темная темы.
- **Адаптивность**: Интерфейс полностью адаптирован под мобильные устройства и десктопы.
- **Анимации**: Анимации через `Transition` и `TransitionGroup`.

## 🔧 Технические особенности

Проект построен с использованием Vue 3:

- **Архитектура**: Структура проекта модульная, где код разделен на слои (`app`, `pages`, `widgets`, `features`, `entities`, `shared`), что упрощает масштабирование и поддержку.
- **Слой API**: Реализован паттерн `Networker` — класс-обертка над `fetch` для централизованной обработки запросов, перехвата ошибок и автоматического обновления токенов (refresh token flow).
- **State Management**: Глобальное состояние управляется через **Vuex 4** с модульной структурой (`user`, `cart`, `settings`), что обеспечивает четкое разделение ответственности.
- **Компоненты**: Использование "глупых" (UI) и "умных" (Smart) компонентов. Базовые UI-элементы (`ButtonUI`, `InputUI`) изолированы и переиспользуемы.
- **Стилизация**: Использование нативного CSS.

## 🛠 Стек технологий

- **Framework**: Vue 3 (Options API)
- **State Management**: Vuex 4
- **Routing**: Vue Router 4
- **Build Tool**: Vite
- **API**: [DummyJSON](https://dummyjson.com) (Auth, Products, Users)
- **Linting**: ESLint, Prettier

## 📦 Установка и запуск

1. Клонируйте репозиторий и установите зависимости:
   ```bash
   npm install
   ```
2. Запустите локальный сервер разработки:
   ```bash
   npm run dev
   ```
3. Сборка для продакшена:
   ```bash
   npm run build
   ```

## 🔑 Тестовый вход

Для доступа к функциям личного кабинета используйте моковые учетные данные:

- **Логин**: `emilys`
- **Пароль**: `emilyspass`

> **Примечание**: Регистрация новых пользователей не работает, форма регистрации существует для полноты картины. DummyJSON не позволяет регистрировать пользователей.

## 📂 Структура проекта (src)

```text
src/
├── app/                  # Системный слой приложения
│   ├── assets/           # Глобальные стили, переменные, изображения, шрифты
│   ├── layout/           # Основные макеты страниц (Header)
│   └── providers/        # Провайдеры логики
│       ├── router/       # Конфигурация маршрутизации
│       └── store/        # Vuex Store и его модули (cart, user, settings)
├── components/
│   ├── ui/               # Атомарные компоненты (ButtonUI, InputUI, ModalUI)
│   └── ...               # Общие компоненты (IconSVG, ErrorBubble)
├── pages/                # Страницы приложения (Views)
│   ├── AccountView/
│   ├── CartView/
│   ├── CatalogueView/
│   ├── HomeView/
│   ├── LoginView/
│   └── ProductView/
├── services/             # Слой работы с внешними данными
│   ├── api/              # Методы API (auth, vehicles, reviews) и Networker
│   └── icons/            # Вспомогательный сервис
└── widgets/              # Самостоятельные блоки интерфейса
    └── TheNavigation.vue # Навигационная панель
```

## Lighthouse и валидация

### Как запустить тесты

Для запуска полного цикла валидации выполните команду:

```bash
npm run audit
```

### Отчет Lighthouse

Для генерации детального отчета Lighthouse выполните:

```bash
npx unlighthouse
```

Готовые отчёты расположены в корневой директории в виде `.html` файлов.
